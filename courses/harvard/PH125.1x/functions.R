log(8)

## help for functions
help(log) # or ?log

## show arguments
args(log)

## assign argument with =
log(x=8, base=2)

## arguments in order
log(8, 2)

## show available datasets
data()

## pre-built objects
pi
Inf # infinity

## assign expressions to variables
a <- 1
b <- 1
c <- -1
solution_1 <- (-b + sqrt(b^2 - 4*a*c)) / (2*a)
solution_2 <- (-b - sqrt(b^2 - 4*a*c)) / (2*a)